<Window x:Class="Bullseye_Calculator.WPF.View.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:viewmodel="clr-namespace:Bullseye_Calculator.WPF.ViewModel"
		d:DataContext="{d:DesignInstance Type=viewmodel:CalculatorViewModel}"
		mc:Ignorable="d"
		Title="Bullseye Calculator" Height="340" Width="260" MinHeight="340" MinWidth="260" >
	<Grid Background="{StaticResource BackgroundBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="3*" />
			<RowDefinition />
			<RowDefinition Height="6*" />
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Style="{StaticResource RoundedBorderStyle}" >
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="2*" />
					<RowDefinition />
				</Grid.RowDefinitions>

				<Label Grid.Row="0" Content="{Binding Input}" />
				<Label Grid.Row="1" Content="{Binding Result}" />
			</Grid>
		</Border>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="3*" />
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0" Command="{Binding ShowStepsCommand}">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource SpecialButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding ShowSteps}" Value="False">
								<Setter Property="Content" Value="🔽Steps" />
							</DataTrigger>
							<DataTrigger Binding="{Binding ShowSteps}" Value="True">
								<Setter Property="Content" Value="🔼Steps" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
			<Button Grid.Column="1" Content="S" Command="{Binding StandardModeCommand}">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource LightButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Standard">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
			<Button Grid.Column="2" Content="[]" Command="{Binding EuclideanModeCommand}">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource LightButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Matrix">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
			<Button Grid.Column="3" Content="I" Command="{Binding InterpolationModeCommand}">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource LightButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Interpolation">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
			<Button Grid.Column="4" Content="∫" Command="{Binding IntegralModeCommand}">
				<Button.Style>
					<Style TargetType="Button" BasedOn="{StaticResource LightButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Integral">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Button.Style>
			</Button>
			<Button Grid.Column="5" Content="⚙️" Command="{Binding ShowOptionsCommand}">
				<Button.Style>
				<Style TargetType="Button" BasedOn="{StaticResource SpecialButton}">
					<Setter Property="IsEnabled" Value="True" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding CurrentMode}" Value="Options">
							<Setter Property="IsEnabled" Value="False" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
				</Button.Style>
			</Button>
		</Grid>

		<ListView Grid.Row="2" ItemsSource="{Binding Evaluation}" />

		<Grid Grid.Row="2" x:Name="Keys">
			<Grid.ColumnDefinitions>
				<ColumnDefinition>
					<ColumnDefinition.Style>
						<Style TargetType="ColumnDefinition">
							<Setter Property="Width" Value="auto" />
							<Style.Triggers>
								<DataTrigger Binding="{Binding CurrentMode}" Value="Matrix">
									<Setter Property="Width" Value="1*" />
								</DataTrigger>
								<DataTrigger Binding="{Binding CurrentMode}" Value="Interpolation">
									<Setter Property="Width" Value="1*" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</ColumnDefinition.Style>
				</ColumnDefinition>
				<ColumnDefinition Width="5*" />
			</Grid.ColumnDefinitions>

			<Grid Grid.Column="0" x:Name="MatrixKeys">
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>

				<Button Grid.Row="0" Content="[" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="[" />
				<Button Grid.Row="1" Content="]" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="]" />
				<Button Grid.Row="2" Content="{Binding ColumnSeparator}" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="{Binding ColumnSeparator}" />
				<Button Grid.Row="3" Content="{Binding RowSeparator}" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="{Binding RowSeparator}" />
				<Button Grid.Row="4" Content="[⋱]" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="diag(" />
				<Button Grid.Row="5" Content="[]⁻¹" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="inv(" />

				<Grid.Style>
					<Style TargetType="Grid">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Matrix">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Grid.Style>
			</Grid>

			<Grid Grid.Column="0" x:Name="InterpolationKeys">
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>

				<Button Grid.Row="0" Content="[" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="[" />
				<Button Grid.Row="1" Content="]" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="]" />
				<Button Grid.Row="2" Content="{Binding ColumnSeparator}" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="{Binding ColumnSeparator}" />
				<Button Grid.Row="3" Content="{Binding RowSeparator}" Style="{StaticResource SpecialButton}" Command="{Binding InputCommand}" CommandParameter="{Binding RowSeparator}" />

				<Grid.Style>
					<Style TargetType="Grid">
						<Setter Property="Visibility" Value="Collapsed" />
						<Style.Triggers>
							<DataTrigger Binding="{Binding CurrentMode}" Value="Interpolation">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Grid.Style>
			</Grid>

			<Grid Grid.Column="1" x:Name="StandardKeys">
				<Grid.RowDefinitions>
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition />
					<ColumnDefinition />
					<ColumnDefinition />
					<ColumnDefinition />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<Button Grid.Row="5" Grid.Column="2" Content="0" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="0" />
				<Button Grid.Row="4" Grid.Column="1" Content="1" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="1" />
				<Button Grid.Row="4" Grid.Column="2" Content="2" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="2" />
				<Button Grid.Row="4" Grid.Column="3" Content="3" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="3" />
				<Button Grid.Row="3" Grid.Column="1" Content="4" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="4" />
				<Button Grid.Row="3" Grid.Column="2" Content="5" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="5" />
				<Button Grid.Row="3" Grid.Column="3" Content="6" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="6" />
				<Button Grid.Row="2" Grid.Column="1" Content="7" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="7" />
				<Button Grid.Row="2" Grid.Column="2" Content="8" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="8" />
				<Button Grid.Row="2" Grid.Column="3" Content="9" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter="9" />

				<Button Grid.Row="0" Grid.Column="5" Content="%" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="%" />
				<Button Grid.Row="1" Grid.Column="5" Content="/" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="/" />
				<Button Grid.Row="2" Grid.Column="5" Content="×" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="*" />
				<Button Grid.Row="3" Grid.Column="5" Content="–" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="-" />
				<Button Grid.Row="4" Grid.Column="5" Content="+" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="+" />
				<Button Grid.Row="5" Grid.Column="5" Content="=" Style="{StaticResource SpecialButton}" Command="{Binding EvaluateCommand}" />

				<Button Grid.Row="5" Grid.Column="1" Content="␣" Style="{StaticResource LightButton}" Command="{Binding InputCommand}" CommandParameter=" " />
				<Button Grid.Row="5" Grid.Column="3" Content="{Binding Separator}" Command="{Binding InputCommand}" Style="{StaticResource LightButton}" 
				CommandParameter="{Binding Separator}" />

				<Button Grid.Row="0" Grid.Column="1" Content="(" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="(" />
				<Button Grid.Row="0" Grid.Column="2" Content=")" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter=")" />

				<Expander Grid.Row="0" Grid.Column="0" Header="⨍">
					<StackPanel>
						<Button Content="|x|" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="abs("/>
						<Button Content="!x" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="fact(" />
					</StackPanel>
				</Expander>
				<Expander Grid.Row="1" Grid.Column="0" Header="📐">
					<StackPanel>
						<Button Content="sin" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="sin(" />
						<Button Content="cos" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="cos(" />
						<Button Content="eˣ" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="exp(" />
						<Button Content="ln" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="ln(" />
					</StackPanel>
				</Expander>
				
				<Button Grid.Row="2" Grid.Column="0" Content="x²" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="^2" />
				<Button Grid.Row="3" Grid.Column="0" Content="xⁿ" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="^" />
				<Button Grid.Row="4" Grid.Column="0" Content="√x" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="2|" />
				<Button Grid.Row="5" Grid.Column="0" Content="ⁿ√x" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="|" />

				<Button Grid.Row="1" Grid.Column="1" Content="π" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="pi" />
				<Button Grid.Row="1" Grid.Column="2" Content="e" Style="{StaticResource DarkButton}" Command="{Binding InputCommand}" CommandParameter="e" />

				<Button Grid.Row="0" Grid.Column="3" Content="C" Style="{StaticResource DarkButton}" Command="{Binding ClearCommand}" />
				<Button Grid.Row="1" Grid.Column="3" Content="⌫" Style="{StaticResource DarkButton}" Command="{Binding BackSpaceCommand}" />
			</Grid>
		</Grid>
	</Grid>
</Window>